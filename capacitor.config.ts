import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.securefinance.admin',
  appName: 'SecurePRO Admin',
  webDir: 'dist'
};

export default config;
